---
title: Command reference
description: All twelve reactor commands, their key flags, and the documented exit codes.
---

# Command reference

The command is `reactor`. Run `reactor <command> --help` for the full options of any command. Run `reactor --version` (or `-v`) for the CLI version.

## Global flags

These are honored by every command and override `reactor.yml`.

| Flag | Meaning |
| --- | --- |
| `--state-dir <path>` | The durable state directory (default `./.reactor`). |
| `--project <dir>` | The project directory containing `reactor.yml` (default `.`). |
| `--json` | Machine-readable JSON output. |
| `--offline` | Force offline mode (sets `REACTOR_OFFLINE=1`). |

## Commands

The "Live?" column marks which commands reach the model surface. Live commands need `OPENROUTER_API_KEY` plus the `@openai/agents` and `zod` peer deps. Every other command runs fully offline.

| Command | Live? | What it does |
| --- | --- | --- |
| `reactor init [dir]` | offline | Scaffold a minimal project: a gateway, a responsibility, `reactor.yml`, `.gitignore`, and a `README.md`. Flag: `--force`. |
| `reactor doctor [--live]` | offline (`--live` probes) | Report node, SDK, key/deps, offline mode, sandbox, state-dir, and IR health. `--live` runs one smoke render. |
| `reactor compile [--force] [--check]` | live (cache hit and `--check` are offline) | Run the compile sessions into the IR cache. `--check` exits non-zero when stale. |
| `reactor run` | live | Ensure the IR is fresh, boot, drain to quiescence, report, and exit. |
| `reactor serve [--http <port>] [--concurrency <n>] [--poll-interval <ms>]` | live | Boot the durable host, the continuity loop, and the HTTP surface. |
| `reactor trigger <node> [--data <json>\|@file]` | live | Trigger a node with an external wake (one-shot mount, or `POST` to a daemon). |
| `reactor status` | offline | Standing compile cost beside live run cost plus dispositions. |
| `reactor topology` | offline | Print the compiled DAG: nodes with wake source, and resolved edges. |
| `reactor inspect <node> [--strict]` | offline | A node's topology position, fingerprints, last receipt, and chain. |
| `reactor logs [--node <node>]` | offline | The receipt stream, optionally filtered to one node. |
| `reactor trace [<node>]` | offline | Each node's receipt chain: wake to disposition. |
| `reactor receipts [list\|verify\|cost] [--node <node>]` | offline | Audit the receipt trail. `verify` exits non-zero on a broken chain. |

## Documented exit codes

The CLI uses stable, documented exit codes so it composes in CI and scripts.

| Code | Meaning |
| --- | --- |
| `0` | Success, or healthy. A help or version display also exits `0`. |
| `1` | A reported failure with an actionable message: a stale cache (`compile --check`), a broken receipt chain (`receipts verify`, `inspect --strict`), no contracts found, a bad config, an unhealthy environment (`doctor`), a missing live key or dep (`--live`), or a connector or render error. |
| `2` | A usage error: an unknown command or flag, or a missing argument, surfaced by the arg parser. |

Failure modes carry actionable messages. A missing live key points you to set `OPENROUTER_API_KEY`. A `mode: docker` config with no daemon tells you to install or start Docker, or that renders fall back to the bounded shell. A stale cache tells you to run `reactor compile`.

## See also

- [Quickstart](/cli/quickstart) walks the commands end to end.
- [Compile, run, serve](/cli/compile-run-serve) covers the three core verbs in depth.
- [Observability](/cli/observability) covers `status`, `inspect`, `topology`, `logs`, `trace`, and `receipts`.
